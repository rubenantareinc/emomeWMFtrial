"use client";
import Image from "next/image";
import { ChevronLeft, ChevronRight } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { challenges } from "./content";

export function ChallengeCarousel(){const [i,setI]=useState(0); const [paused,setPaused]=useState(false); const reduced=useRef(false); useEffect(()=>{reduced.current=matchMedia('(prefers-reduced-motion: reduce)').matches},[]); useEffect(()=>{if(paused||reduced.current)return; const t=setInterval(()=>setI(v=>(v+1)%challenges.length),4200); return()=>clearInterval(t)},[paused]); const go=(n:number)=>setI((n+challenges.length)%challenges.length); return <section id="challenges" className="overflow-hidden py-16 md:py-24"><div className="mx-auto max-w-7xl px-4 md:px-6"><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-black uppercase tracking-[.24em] text-rose-500">Challenges</p><h2 className="mt-3 text-4xl font-black tracking-tight text-zinc-950 md:text-5xl">Small challenges. Real conversations.</h2><p className="mt-4 max-w-2xl text-zinc-600">Each Emome challenge gives you a simple way to connect, understand, appreciate, or repair.</p></div><div className="flex gap-2"><button aria-label="Previous challenge" onClick={()=>go(i-1)} className="grid size-12 place-items-center rounded-full border bg-white shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-rose-500"><ChevronLeft/></button><button aria-label="Next challenge" onClick={()=>go(i+1)} className="grid size-12 place-items-center rounded-full border bg-white shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-rose-500"><ChevronRight/></button></div></div><div onMouseEnter={()=>setPaused(true)} onMouseLeave={()=>setPaused(false)} className="relative" aria-roledescription="carousel"><div className="flex touch-pan-y transition-transform duration-700 ease-out" style={{transform:`translateX(-${i*100}%)`}}>{challenges.map(c=><article key={c.slug} className="min-w-full px-1"><div className="grid items-center gap-6 rounded-[2.5rem] border border-white/80 bg-white/82 p-4 shadow-[0_22px_70px_rgba(120,50,30,.12)] md:grid-cols-[.9fr_1.1fr] md:p-8"><div className="overflow-hidden rounded-[2rem] bg-gradient-to-br from-rose-50 to-orange-50"><Image src={c.image} alt={`${c.title} challenge artwork`} width={900} height={900} className="aspect-square w-full object-contain"/></div><div className="p-2 md:p-6"><span className="rounded-full bg-rose-100 px-3 py-1 text-xs font-black uppercase tracking-[.18em] text-rose-700">{c.category} • {c.time}</span><h3 className="mt-5 text-4xl font-black tracking-tight text-zinc-950 md:text-6xl">{c.title}</h3><p className="mt-5 max-w-xl text-lg leading-8 text-zinc-600">{c.benefit}</p></div></div></article>)}</div><div className="mt-6 flex justify-center gap-2">{challenges.map((c,n)=><button key={c.slug} aria-label={`Show ${c.title}`} onClick={()=>go(n)} className={`h-2.5 rounded-full transition-all ${n===i?'w-10 bg-rose-500':'w-2.5 bg-rose-200'}`}/>)}</div></div></div></section>}
